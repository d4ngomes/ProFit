@model IEnumerable<ProFit.Web.Models.Horario>

@{
    ViewData["Title"] = "Horários";
}

<div class="row mt-4 mb-4">
    <div class="col-sm">
        <h2>Lista de Horários</h2>
    </div>
    <div class="col-sm">
        <a class="btn btn-primary" asp-action="Create"><i class="far fa-clock"></i> Novo Horário</a>
    </div>
</div>

@if (TempData["Msg"] != null)
{
    <div class="row mt-4 mb-4 col-md-6">
        <div class="alert alert-success" role="alert">
            @TempData["Msg"]
        </div>
    </div>
}

<div class="row col-md-6 mb-5">
    @if (Model.Count() > 0)
    {
        <div class="card">
            <div class="card-body">
                <table class="table table-striped border">
                    <thead>
                        <tr>
                            <th scope="col">@Html.DisplayNameFor(m => m.Hora)</th>
                            <th scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(m => item.Hora)</td>
                                <td>
                                    <a class="btn btn-success btn-sm" asp-action="Edit" asp-route-id="@item.Id"><i class="fas fa-edit"></i></a>
                                    <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id"><i class="far fa-trash-alt"></i></a>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <p>Nenhum horário cadastrado.</p>
    }
</div>

